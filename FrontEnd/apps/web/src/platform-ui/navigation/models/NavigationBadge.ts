/**
 * NavigationBadge — re-exported from the theme contract so platform navigation
 * authoring code imports it from one place (`platform-ui/navigation/models`).
 *
 * The badge is a presentational concern shared verbatim between the platform
 * (which supplies it) and the theme (which renders it).
 */
export type { NavigationBadge, NavigationBadgeVariant } from '@/theme/contracts';
