# Invitations operations

- Grant `user:invite` to Admin/Manager roles via `auth.permission_grants`.
- Monitor `UserInvitedEvent` handler failures in Seq.
- Expired invitations return validation error on accept.
